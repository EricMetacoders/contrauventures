before_script:
  - apt-get update -qq
  - apt-get install -qq git
  # Setup SSH deploy keys
  - 'which ssh-agent || ( apt-get install -qq openssh-client )'
  - eval $(ssh-agent -s)
  - echo "$SSH_PRIVATE_KEY_TO_BLUE_HOST" | tr -d '\r' | ssh-add -
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'

deploy_new_dns:
  stage: deploy
  environment:
    name: new_dns
    url: contrau.ventures2
  script:
    - ssh contrauv@162.241.253.222 "cd /home1/contrauv/public_html/contra_react_wordpress && git checkout new_dns && git pull origin new_dns && exit"
  only:
    - new_dns